[package]
name = "cucumber_tests"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }

concerto_core = { package = "concerto-core", path = "../../../../../../concerto-core" }
cucumber = "0.21"

[dev-dependencies]
cucumber = "0.21"
